[pytest]
testpaths = tests
python_files = test_*.py
# app.py reassigns sys.stdout (Windows UTF-8 console) at import; disable pytest
# output capture so that reassignment doesn't collide with the capture teardown.
addopts = -s
